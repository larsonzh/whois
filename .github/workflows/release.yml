name: Release - Disabled (replaced by build.yml)

on:
  workflow_call:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use build.yml release job triggered by tag push or manual dispatch."
